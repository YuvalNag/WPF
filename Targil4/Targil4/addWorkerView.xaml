<UserControl x:Class="Targil4.addWorkerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
              xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             xmlns:local="clr-namespace:Targil4"
              xmlns:DP="clr-namespace:DataProtocol;assembly=DataProtocol"
            
             mc:Ignorable="d" 
             >
    <UserControl.Resources>
        <DP:Worker x:Key="worker"/>
    </UserControl.Resources>
    <Grid>
        <StackPanel Orientation="Horizontal">
            <TextBox Text="{Binding firstName}" Controls:TextBoxHelper.UseFloatingWatermark="True"  Controls:TextBoxHelper.Watermark="First Name" Margin="8" Controls:TextBoxHelper.WatermarkAlignment="Center"/>
            <TextBox Text="{Binding lastName}" Controls:TextBoxHelper.UseFloatingWatermark="True" Controls:TextBoxHelper.Watermark="Last Name" Margin="8" />
            <ComboBox ItemsSource="{Binding educuationList}" Margin="8"/>
            <Button Margin="8" Style="{StaticResource MetroCircleButtonStyle}" Command="{Binding addCommand}" CommandParameter="{Binding }">
                <Image Source="Assets/submit.png" Height="40"/>
            </Button> 
        </StackPanel>


    </Grid>
</UserControl>
